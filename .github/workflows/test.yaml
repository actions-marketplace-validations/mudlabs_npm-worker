name: Test Worker

on:
  push:
    paths: 
      - test/npm.?worker.config.ya?ml
   
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Worker
        uses: ./
        with:
          config: ./test/
