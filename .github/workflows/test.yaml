name: Test Worker

on:
  push:
    paths:
      - ./test/**
   
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Worker
        uses: ./
        with:
          config: ./test/npmworker.config.yaml
